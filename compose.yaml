x-logging: &logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

services:
  app:
    build: .
    ports:
      - "5097:5097"
    env_file:
      - .env
    environment:
      - PORT=5097
    restart: unless-stopped
    init: true
    logging: *logging
